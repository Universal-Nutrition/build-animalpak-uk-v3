<script src="{{ 'product-faq.js' | asset_url }}" defer="defer"></script>

{% liquid
  assign color = section.settings.bg_color
  assign sectionHeading = section.settings.heading
  assign faqOneTitle = product.metafields.custom.faq_1_title
  assign faqOneBody = product.metafields.custom.faq_1_body
  assign faqTwoTitle = product.metafields.custom.faq_2_title
  assign faqTwoBody = product.metafields.custom.faq_2_body
  assign faqThreeTitle = product.metafields.custom.faq_3_title
  assign faqThreeBody = product.metafields.custom.faq_3_body
%}
{% if faqOneTitle != blank %}
<div id="section-product-faq" class="product-faq w-full " style="background-color: {{color}}">
  <div class="container">
  
    {% if sectionHeading != blank %}
      <h2 class="text-6xl md:text-[85.6px] uppercase text-left sm:text-center text-primary mb-6 sm:mb-12 font-bold leading-[0.8em]">{{ sectionHeading }}</h2>
    {% endif %}
  <div class="accordion-wapper" style="background: none;">
      <div class="accordion">
        {% for i in (1..12) %}
          {% assign title = 'faq_' | append: i | append: '_title' %}
          {% assign body = 'faq_' | append: i | append: '_body' %}
          {% if product.metafields.custom[title] != blank %}
            <details {% unless forloop.last %}class="border-b border-[#000]"{% endunless %}>
                <summary class="bg-yellow">
                  <div class="" style="padding: .5em 1em;">
                      <p class="w-full flex justify-between items-center h4 accordion__title text-base py-6 cursor-pointer font-sans font-bold tracking-normal text-primary">
                        {{product.metafields.custom[title]}}
                        {% render 'icon-accordion' %}
                      </p>
                  </div>
                </summary>
                <div class="accordion__content text-base rte pb-[15px] pr-10" >
                  {{product.metafields.custom[body]}}
                </div>
            </details>
          {% endif %}
        {% endfor %}
        
      </div>
  </div>
    </div>
  </div>
{% endif %}


<style>
details[open]>summary .icon-caret {
  transform: rotate(180deg);
}
#section-product-faq h2 {
  justify-content: center;
  background: none;
  color: #FFF;
  margin: 0 auto;
  line-height: 1;
}
#section-product-faq .container {
  padding-bottom: 85px;
  padding-top: 60px;
}

.product-faq h2:after {
  display: none;
}

@media only screen and (max-width: 768px) {
  #section-product-faq .container {
    padding-bottom: 30px;
    padding-top: 0;
  }
}

details {
  background-color: #FFF;
}
.accordion__content.rte {
  padding: 1em;
}
@media only screen and (min-width: 768px) {
  #section-product-faq h2  {
    font-size: 60px;
  }
}

@media screen and (max-width: 767px) {
  #section-product-faq h2  {
    padding-top: 1em;
  }
}
</style>

{% if product.metafields.custom.faq_1_title != blank %}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {% for i in (1..12) %}
      {% assign title = 'faq_' | append: i | append: '_title' %}
      {% assign body = 'faq_' | append: i | append: '_body' %}
      {% if product.metafields.custom[title] != blank  %}
        {% unless forloop.first %},{% endunless %}
        {
          "@type": "Question",
          "name": "{{product.metafields.custom[title] | replace: '"', '\"'}}",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "{{product.metafields.custom[body] | replace: '"', '\"'}}"
          }
      }
      {% endif %}
    {% endfor %}
  ]
}
</script>
{% endif %}

{% schema %}
{
  "name": "Product - FAQ Section",
  "class": "shopify-section-vue",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "FAQs"
    },
    {
      "type": "color",
      "id": "bg_color",
      "default": "#FFFFFF",
      "label": "Background Color of section"
    }
  ],
  "presets": [
    {
      "name": "FAQ Section",
      "category": "Section"
    }
  ],
  "templates": ["product"]
}
{% endschema %}
